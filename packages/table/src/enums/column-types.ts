export enum ColumnTypes {
  String = 'string',
  Textarea = 'textarea',
  Integer = 'integer',
  Float = 'float',
  Boolean = 'boolean',
  Select = 'select',
  Object = 'object',
  Date = 'date',
  DateTime = 'date-time',
  Image = 'image',
}
