import {
  Connection as TypeormConnection,
  ConnectionOptions as TypeormConnectionOptions
} from 'typeorm'

export type SupportedConnection = TypeormConnection
export type SupportedConnectionOptions = TypeormConnectionOptions
